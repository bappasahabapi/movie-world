"use strict";
// import request from 'supertest';
// import app from '../src/testApp'; // lightweight app export for tests
// describe('Movies API', () => {
//   it('lists movies and supports title filter', async () => {
//     const res = await request(app).get('/api/movies').query({ title: 'man' });
//     expect(res.status).toBe(200);
//     expect(Array.isArray(res.body.items)).toBe(true);
//   });
// });
